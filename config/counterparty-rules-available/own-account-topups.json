{
  "description": "Own-account card top-up link mapping (Option A — JSON config). Shipped as an anonymised example. Each entry maps a funding-bank card to a destination Revolut account. Real values live in config/counterparty-rules-enabled/own-account-topups.json (operator-managed, not committed). See DECISIONS.md — GH #16 for the documented upgrade path to a DB table if this file outgrows config.",
  "links": [
    {
      "funding_bank_slug": "example-bank",
      "funding_card_last4": "1234",
      "funding_marker": "Revolut",
      "destination_account_ref": "Example Revolut EUR",
      "apple_pay_tokens": ["5678"],
      "amount_tolerance_days": 3
    }
  ]
}
